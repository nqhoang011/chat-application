server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: chatapp
  datasource:
    url: jdbc:postgresql://localhost:5432/chat_app
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
